import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <App />
);

/*
jsx = usar html no

Componentes: é uma estrutura de código que retorna HTML, CSS, ou JS
Propriedades: informações que um componente pai passa para um comp. filho
Estado: informação que o componente armazena e manipula - useState (importa por {} no react)
*/